public class ternaryop {
    public static void main(String[] args) {
        // Ternary operator - a conditional operator that takes 3 operands and evaluates an expression based on a condition.
        int income = 120_000;
        String className = income > 100_000 ? "First" : "Economy"; // Condition - if - else
    }
}
